import React from 'react'

export default function TrackPage() {
  // 1. ID coming in is from spotify
  // 2. Check if I have it in my DB
  // 3. If I have it, use my data
  // 4. If I don't have it, fetch from spotify and store in my DB 
  // 4a. storing involves fetching audio features, and music-brainz data
  // 5. Then use that data to render the page
  return (
    <div>trackPage</div>
  )
}
